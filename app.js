// write code here
if (' ') alert('Yes');
else alert('Nooo');

// ตัว code สามารถ run ได้ โดยให้ผลลัพธ์เป็นการ alert คำว่า 'Yes' เนื่องจากเงื่อนไขของ if เป็น true (boolean ของ string ที่มี spacebar อยู่ข้างในไม่ใช่ string ว่าง, ผลลัพธ์จึงเป็น true)